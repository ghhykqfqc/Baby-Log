/* pages/share/share.wxss - 每日小结（重构版） */

.page-share {
  min-height: 100vh;
  padding: 24rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* ===== 游客拦截占位（登录后才能生成分享卡） ===== */
.guest-block {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 40rpx;
  text-align: center;
}

.guest-block-icon {
  font-size: 96rpx;
  line-height: 1;
  margin-bottom: 32rpx;
}

.guest-block-title {
  font-size: 34rpx;
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: 16rpx;
}

.guest-block-sub {
  font-size: 26rpx;
  color: var(--color-text-secondary);
  line-height: 1.6;
  margin-bottom: 48rpx;
}

.guest-block-btn {
  width: 320rpx;
  height: 88rpx;
  border-radius: 44rpx;
  background: var(--color-primary);
  color: #FFFFFF;
  font-size: 30rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: var(--shadow-button);
  margin-bottom: 24rpx;
}

.guest-block-back {
  padding: 16rpx 32rpx;
  font-size: 26rpx;
  color: var(--color-text-secondary);
}

/* ===== 顶部标题 ===== */
.share-header {
  text-align: center;
  margin-bottom: 24rpx;
}

.title {
  font-size: var(--font-xl);
  font-weight: 600;
  color: var(--color-text-primary);
}

.subtitle {
  font-size: var(--font-sm);
  color: var(--color-text-secondary);
  margin-top: 8rpx;
}

/* ===== 主题切换 ===== */
.theme-tabs {
  display: flex;
  background: var(--color-bg-card);
  border-radius: var(--radius-md);
  padding: 8rpx;
  gap: 4rpx;
  margin-bottom: 24rpx;
  box-shadow: var(--shadow-card);
  width: 100%;
  max-width: 600rpx;
  box-sizing: border-box;
}

.theme-tab {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 16rpx 8rpx;
  border-radius: var(--radius-sm);
  transition: all 0.2s;
}

.theme-tab.active {
  background: var(--color-primary);
  color: #FFFFFF;
  box-shadow: 0 4rpx 16rpx rgba(212, 184, 150, 0.4);
}

.tab-icon {
  font-size: 36rpx;
  margin-bottom: 4rpx;
}

.tab-label {
  font-size: var(--font-xs);
  color: var(--color-text-secondary);
}

.theme-tab.active .tab-label {
  color: #FFFFFF;
}

/* ===== 卡片容器 ===== */
.canvas-container {
  width: 100%;
  max-width: 600rpx;
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: 0 12rpx 40rpx rgba(180, 150, 110, 0.25);
  background: #FAF6F0;
}

.share-canvas {
  width: 600rpx;
  height: 1066rpx; /* 750x1334 按 600 宽度等比缩放 */
}

/* ===== 加载状态 ===== */
.loading-area {
  width: 100%;
  max-width: 600rpx;
  height: 1066rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: var(--color-bg-card);
  border-radius: var(--radius-lg);
}

.loading-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: var(--color-primary);
  margin: 4rpx;
  display: inline-block;
  animation: dotBounce 1.4s ease-in-out infinite;
}

.loading-dot:nth-child(2) { animation-delay: 0.16s; }
.loading-dot:nth-child(3) { animation-delay: 0.32s; }

@keyframes dotBounce {
  0%, 80%, 100% { transform: scale(0.6); opacity: 0.4; }
  40% { transform: scale(1); opacity: 1; }
}

.loading-text {
  margin-top: 16rpx;
  font-size: var(--font-sm);
  color: var(--color-text-hint);
}

/* ===== 数据来源提示 ===== */
.data-source {
  margin: 12rpx 0 16rpx;
  padding: 8rpx 20rpx;
  background: var(--color-primary-bg);
  border-radius: 20rpx;
}

.source-text {
  font-size: var(--font-xs);
  color: var(--color-text-secondary);
}

/* ===== 操作按钮 ===== */
.actions {
  width: 100%;
  max-width: 600rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-top: 8rpx;
}

.action-btn {
  padding: 24rpx;
  border-radius: var(--radius-md);
  text-align: center;
  font-size: var(--font-md);
  font-weight: 600;
}

.action-btn.primary {
  background: var(--color-primary);
  color: #FFFFFF;
  box-shadow: var(--shadow-button);
}

.action-btn.secondary {
  background: var(--color-bg-card);
  color: var(--color-text-primary);
  border: 1rpx solid var(--color-divider);
}

.tip {
  margin-top: 24rpx;
  font-size: var(--font-xs);
  color: var(--color-text-hint);
  text-align: center;
  padding-bottom: 40rpx;
}

/* 分享落地引导条（游客/访客从分享卡进入时显示） */
.shared-banner {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 24rpx 28rpx;
  margin-bottom: 24rpx;
  background: linear-gradient(135deg, #FCF6EC, #FAF0E0);
  border: 1rpx solid rgba(212, 184, 150, 0.4);
  border-radius: var(--radius-lg);
}
.shared-banner-icon {
  font-size: 48rpx;
  line-height: 1;
  flex-shrink: 0;
}
.shared-banner-text {
  flex: 1;
  min-width: 0;
}
.shared-banner-title {
  font-size: var(--font-md);
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: 4rpx;
}
.shared-banner-sub {
  font-size: var(--font-xs);
  color: var(--color-text-secondary);
  line-height: 1.5;
}
.shared-banner-actions {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  flex-shrink: 0;
}
.shared-banner-btn {
  padding: 12rpx 28rpx;
  border-radius: 32rpx;
  font-size: var(--font-sm);
  font-weight: 600;
  text-align: center;
  line-height: 1;
}
.shared-banner-btn.primary {
  background: var(--color-primary);
  color: #FFFFFF;
}
.shared-banner-btn.ghost {
  background: #FFFFFF;
  color: var(--color-primary);
  border: 1rpx solid var(--color-primary);
}
